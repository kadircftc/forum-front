 <header 
      className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300  ${
        isScrolled 
          ? 'bg-white shadow-md py-2' 
          : 'bg-gradient-to-r from-primary-900 to-secondary-900 text-white py-6'
      }`}
    >


    home page

    import { BrainCog, LightbulbIcon, Sparkles, Star, TrendingUp, Users } from 'lucide-react';
import React from 'react';
import { useNavigate } from 'react-router-dom';
import CategoryList from '../components/categories/CategoryList';
import Layout from '../components/layout/Layout';
import PromptList from '../components/prompts/PromptList';

const HomePage: React.FC = () => {
  const navigate = useNavigate();

  return (
    <Layout>
      {/* Hero Section */}
      <div className="relative bg-gradient-to-r from-primary-900 via-secondary-900 to-accent-900 text-white overflow-hidden">
        {/* Animated Background Elements */}
        <div className="absolute inset-0 overflow-hidden">
          <div className="absolute -top-40 -right-40 w-80 h-80 bg-primary-500 rounded-full opacity-20 blur-3xl animate-pulse-slow"></div>
          <div className="absolute -bottom-40 -left-40 w-80 h-80 bg-secondary-500 rounded-full opacity-20 blur-3xl animate-pulse-slow" style={{ animationDelay: '1s' }}></div>
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full bg-gradient-to-r from-primary-500/10 to-secondary-500/10 backdrop-blur-3xl"></div>
        </div>

        <div className="container mx-auto px-4 py-20 relative">
          <div className="flex flex-col md:flex-row items-center justify-between gap-12">
            {/* Hero Content */}
            <div className="md:w-1/2 z-10">
              <div className="flex items-center gap-2 mb-6">
                <Sparkles className="text-yellow-400" size={24} />
                <span className="text-yellow-400 font-medium">AI Prompt Revolution</span>
              </div>
              
              <h1 className="text-4xl md:text-6xl font-bold mb-6 leading-tight">
                Discover & Share
                <span className="block text-transparent bg-clip-text bg-gradient-to-r from-primary-400 to-secondary-400">
                  Powerful AI Prompts
                </span>
              </h1>

              <p className="text-xl text-gray-300 mb-8 max-w-lg">
                Join our community of prompt engineers and unlock the full potential of AI models. Create, share, and discover prompts that deliver exceptional results.
              </p>

              <div className="flex flex-wrap gap-4">
                <button 
                  onClick={() => navigate('/explore')}
                  className="px-8 py-4 bg-white text-primary-900 rounded-full font-semibold hover:bg-gray-100 transition-colors flex items-center"
                >
                  <Star size={20} className="mr-2" />
                  Explore Prompts
                </button>
                <button 
                  onClick={() => navigate('/create')}
                  className="px-8 py-4 bg-white/10 backdrop-blur-sm text-white rounded-full font-semibold hover:bg-white/20 transition-colors flex items-center"
                >
                  <LightbulbIcon size={20} className="mr-2" />
                  Share Your Prompt
                </button>
              </div>

              {/* Stats */}
              <div className="flex gap-8 mt-12">
                <div className="text-center">
                  <div className="text-3xl font-bold mb-1">10K+</div>
                  <div className="text-gray-400">Prompts</div>
                </div>
                <div className="text-center">
                  <div className="text-3xl font-bold mb-1">50K+</div>
                  <div className="text-gray-400">Users</div>
                </div>
                <div className="text-center">
                  <div className="text-3xl font-bold mb-1">100K+</div>
                  <div className="text-gray-400">Generations</div>
                </div>
              </div>
            </div>

            {/* Interactive Demo */}
            <div className="md:w-1/2 flex justify-center">
              <div className="relative max-w-lg w-full">
                {/* Floating Elements */}
                <div className="absolute -top-6 -left-6 w-20 h-20 bg-primary-400 rounded-full opacity-20 animate-pulse-slow"></div>
                <div className="absolute -bottom-8 -right-8 w-24 h-24 bg-secondary-400 rounded-full opacity-20 animate-pulse-slow"></div>
                
                {/* Main Terminal */}
                <div className="bg-gray-900/80 backdrop-blur-xl p-6 rounded-2xl border border-white/10 shadow-2xl">
                  <div className="flex items-center justify-between mb-6">
                    <div className="flex gap-2">
                      <div className="w-3 h-3 bg-accent-500 rounded-full"></div>
                      <div className="w-3 h-3 bg-yellow-500 rounded-full"></div>
                      <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                    </div>
                    <div className="flex items-center gap-2">
                      <BrainCog size={20} className="text-primary-400" />
                      <span className="text-sm text-primary-400 font-medium">GPT-4</span>
                    </div>
                  </div>

                  <div className="space-y-4">
                    <div className="flex items-start gap-3">
                      <div className="flex-shrink-0 w-8 h-8 rounded-full bg-primary-500/20 flex items-center justify-center">
                        <Users size={16} className="text-primary-400" />
                      </div>
                      <div className="flex-grow">
                        <p className="text-white/80">Write a creative story about <span className="bg-primary-500/20 px-2 py-0.5 rounded">time travel</span> and <span className="bg-primary-500/20 px-2 py-0.5 rounded">parallel universes</span>.</p>
                      </div>
                    </div>

                    <div className="flex items-start gap-3">
                      <div className="flex-shrink-0 w-8 h-8 rounded-full bg-secondary-500/20 flex items-center justify-center">
                        <TrendingUp size={16} className="text-secondary-400" />
                      </div>
                      <div className="text-sm text-gray-400">
                        Used by 2,481 people • 98% success rate
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Main Content */}
      <div className="container mx-auto max-w-6xl px-4 py-16">
        <div className="mb-12">
          <h2 className="text-3xl font-bold mb-8">Trending Categories</h2>
          <CategoryList />
        </div>

        <div className="mb-8 flex items-center justify-between">
          <h2 className="text-3xl font-bold">Popular Prompts</h2>
          <button 
            onClick={() => navigate('/explore')}
            className="text-primary-600 hover:text-primary-700 font-medium flex items-center"
          >
            View All
            <svg className="w-5 h-5 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
            </svg>
          </button>
        </div>

        <PromptList />
      </div>
    </Layout>
  );
};

export default HomePage;